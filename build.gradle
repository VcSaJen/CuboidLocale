group 'cuboidLocale'
version '1.0-SNAPSHOT'

apply plugin: 'java'

sourceCompatibility = 1.5

repositories {
    mavenCentral()
}

dependencies {
    
}

sourceSets {
    main {
        java {
            srcDir '/'
            exclude 'build.gradle'
       }
   }
}
